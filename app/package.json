{
  "name": "cf-ddns",
  "version": "1.0.0",
  "main": "dist/index.ts",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.39",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.1"
  },
  "scripts": {
    "build": "tsc",
    "start": "yarn build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
